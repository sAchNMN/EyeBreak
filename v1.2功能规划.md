# EyeBreak v1.2 护眼提醒功能规划

## 1. 产品定位

EyeBreak 是一个本地运行的 Windows 护眼提醒工具。它负责在合适的时间提醒用户休息、展示休息倒计时，并提供暂停、跳过和恢复等低成本操作；用户是否真正放下鼠标、放松眼睛和看向远处，由用户自己决定。

EyeBreak 不负责监控用户，也不强制用户休息。程序不通过摄像头、全局键盘监听、鼠标移动监听或前台窗口判断来证明用户是否真的完成了护眼行为。

## 2. v1.2 目标

本版本只增强已有护眼提醒流程：

1. 阻止非法配置破坏计时器。
2. 让提醒窗口的暂停操作更容易发现和使用。
3. 支持今天剩余时间内不再提醒。
4. 记录用户能看懂的本地使用统计。
5. 区分“倒计时自然结束”和“用户主动跳过”。

## 3. 不在范围内

以下内容明确不开发：

* 番茄钟和专注工作流。
* 账号系统、云同步和网络服务。
* 摄像头、全局键盘钩子和鼠标移动监控。
* 根据用户眼睛方向、坐姿或屏幕距离判断是否有效休息。
* 在线统计、排行榜、复杂日报和周报。
* AI 建议、自动应用识别和团队功能。
* 自动更新和新的第三方运行时依赖。

## 4. 重要指标定义

### 4.1 程序可以记录的指标

```text
提醒次数       实际弹出的计划护眼提醒次数
倒计时完成次数 休息倒计时自然结束次数
跳过次数       用户点击“跳过本次”的次数
暂停次数       用户成功执行普通暂停或“今天不再提醒”的次数
```

建议展示的参考指标：

```text
倒计时完成率 = 倒计时完成次数 ÷ 实际弹出的计划护眼提醒次数
```

这只是“程序记录的倒计时完成率”，不是医学意义上的真实休息完成率，也不能证明用户看向了远处。

### 4.2 结果分类

一次计划提醒的最终结果可以是：

| 结果 | 触发条件 | 是否增加完成次数 |
|---|---|---:|
| 完成 | 倒计时自然结束 | 是 |
| 跳过 | 用户点击“跳过本次”或关闭提醒窗口 | 否 |
| 暂停 | 用户点击普通暂停或“今天不再提醒” | 否 |
| 未结束 | 程序在提醒期间被关闭或异常终止 | 否 |

把提醒切到后台并继续使用电脑，不进行失败检测；只要倒计时自然结束，就按程序行为记录为完成。这个取舍是有意的，因为产品只负责提醒，不负责监督。

### 4.3 计划提醒与立即休息

“立即休息”是用户主动发起的行为，不计入计划提醒完成率的分母。提醒事件需要区分 `scheduled` 和 `manual` 来源；本地统计至少只对 `scheduled` 来源计算提醒次数和完成率。

## 5. 数据与依赖原则

* 配置继续保存到 `config.json`。
* 悬浮窗位置和“今天不再提醒”状态保存到 `app_state.json`。
* 统计数据保存到独立的 `stats.json`。
* JSON 文件使用临时文件写入成功后再替换目标文件，避免写入中断产生半截文件。
* 读取损坏文件时使用备份或默认值，并记录日志；不能把保存失败伪装成成功。
* 不新增运行时依赖，优先使用 Python 标准库和现有 Tkinter、pystray、Pillow。

## 6. 开发阶段

```text
阶段 1：配置合法性与安全保存
    ↓
阶段 2：提醒窗口快捷暂停
    ↓
阶段 3：今天不再提醒
    ↓
阶段 4：本地统计和倒计时结果
    ↓
阶段 5：完整回归、手动验收和发布准备
```

每个阶段都必须独立测试和验收。阶段 1 至 4 完成后才形成 v1.2 功能版本。

## 7. 阶段 1：配置合法性与安全保存

### 7.1 配置合法范围

所有用户输入必须是有限数值。`NaN`、正负 `Infinity`、空字符串、非数字文本和超出范围的数值必须拒绝。

| 配置项 | 合法范围 |
|---|---:|
| 提醒间隔 | `0 < 分钟 <= 1440` |
| 休息时长 | `1 <= 秒 <= 3600` |
| 默认暂停 | `1 <= 分钟 <= 120` |
| 离开检测 | `0 <= 分钟 <= 1440` |

要求：

* 使用 `math.isfinite()` 检查浮点数。
* 校验逻辑集中在配置模块，设置窗口只负责展示错误。
* 非法输入不能修改内存配置，也不能覆盖磁盘上的有效配置。
* 设置窗口显示具体错误原因。
* 合法值保存后立即影响下一轮提醒，但不改变正在进行的普通暂停截止时间。

### 7.2 安全保存

新增一个供配置、状态和统计复用的本地 JSON 原子保存函数，至少保证：

1. 在同目录临时文件中完整写入 JSON。
2. 写入并刷新成功后，用 `os.replace()` 替换目标文件。
3. 替换失败时保留旧文件，不报告保存成功。
4. 配置保存前保留一份 `.bak` 文件，主文件损坏时尝试读取备份。

### 7.3 涉及文件

```text
app/config.py
app/paths.py
app/settings_window.py
app/ui/bridge.py
app/state.py
app/persistence.py
tests/test_config.py
tests/test_settings_window.py
tests/test_state.py
tests/test_persistence.py
```

### 7.4 验收标准

* `NaN` 和 `Infinity` 输入均被拒绝。
* 负数、零值和超大值按配置项规则被拒绝。
* 非法输入不会改变 `config.json`。
* 模拟保存中断后，旧配置仍可读取。
* 损坏的主配置可以从备份恢复。
* 完整测试通过。

## 8. 阶段 2：提醒窗口快捷暂停

### 8.1 操作设计

保留现有滚轮调节能力，但不再把它作为唯一的调节方式。

提醒窗口增加：

```text
[-]  暂停 15 分钟  [+]

[延后 5 分钟] [延后 15 分钟] [延后 30 分钟]
```

要求：

* `-` 和 `+` 每次调整 5 分钟。
* 暂停时长限制在 1 到 120 分钟。
* 到达边界后继续点击不会越界。
* 5/15/30 分钟按钮点击后立即进入对应暂停并关闭提醒窗口。
* 当前暂停时长始终可见。
* `跳过本次`、暂停、退出和倒计时自然结束的行为保持清楚区分。
* 不使用全局输入监听；用户在其他窗口中的键鼠行为不影响提醒结果。

### 8.2 事件语义

自然结束不能继续调用“跳过”路径。提醒窗口至少需要区分：

```text
on_complete()  倒计时自然结束
on_skip()      用户主动跳过或关闭
on_pause(m)    用户暂停
on_exit()      用户退出程序
```

核心引擎分别发布完成和跳过事件，本地统计根据事件更新计数。

### 8.3 涉及文件

```text
app/reminder_window.py
app/core/events.py
app/core/timer_engine.py
app/ui/bridge.py
tests/test_reminder_window.py
tests/test_timer_engine.py
tests/test_bridge.py
```

### 8.4 验收标准

* 用户无需猜测即可看到当前暂停时长。
* 5/15/30 分钟按钮分别产生正确暂停时长。
* `+/-` 在 1 到 120 分钟范围内工作。
* 自然结束发布完成事件，不发布跳过事件。
* 主动跳过只发布跳过事件。
* 现有提醒、暂停、退出流程不回归。

## 9. 阶段 3：今天不再提醒

### 9.1 功能定义

增加“今天不再提醒”操作，作用范围是自动护眼提醒，不是禁止用户主动休息。

入口：

* 系统托盘菜单。
* 当前提醒窗口。

执行后：

* 当天本地时间内不再弹出自动护眼提醒。
* 悬浮窗显示“今日免打扰”和 `--:--`。
* “立即休息”仍可以由用户手动触发。
* 普通“恢复”可以提前解除今天免打扰。
* 该操作计入一次暂停次数。

### 9.2 跨重启和跨午夜

在 `app_state.json` 中保存本地日期标记，例如：

```json
{
  "today_pause_date": "2026-09-02"
}
```

要求：

* 程序当天重启后仍保持免打扰。
* 读取到过期日期时立即清除标记。
* 跨过本地午夜后自动恢复普通提醒，并从新的提醒间隔开始计时。
* 不把跨进程无效的 `time.monotonic()` 时间戳写入磁盘表示“今天结束”。

### 9.3 状态优先级

显示和计时优先级：

```text
退出 > 今天免打扰 > 普通暂停 > 离开 > 全屏 > 正常倒计时
```

今天免打扰期间不弹出自动提醒，也不因离开或全屏状态变化而重新安排提醒。

### 9.4 涉及文件

```text
app/state.py
app/core/state_machine.py
app/core/timer_engine.py
app/core/events.py
app/ui/bridge.py
app/floating_countdown.py
app/reminder_window.py
app/tray.py
tests/test_state.py
tests/test_state_machine.py
tests/test_timer_engine.py
tests/test_bridge.py
tests/test_reminder_window.py
tests/test_tray.py
```

### 9.5 验收标准

* 托盘和提醒窗口都可以执行今天免打扰。
* 当天不会弹出自动提醒。
* 重启程序后当天仍然免打扰。
* 跨午夜后自动恢复提醒。
* “立即休息”仍然可以手动触发。
* 普通暂停、全屏、离开和悬浮窗显示不回归。

## 10. 阶段 4：本地统计和倒计时结果

### 10.1 数据模型

新增本地统计文件 `stats.json`，最小字段为：

```text
reminder_count: int >= 0
completed_count: int >= 0
skip_count: int >= 0
pause_count: int >= 0
```

统计数据必须跨程序重启保留。文件损坏不能阻止程序启动，必要时恢复为零值并记录日志。

### 10.2 统计规则

* 计划提醒弹出时增加 `reminder_count`。
* 计划提醒倒计时自然结束时增加 `completed_count`。
* 用户主动跳过或关闭提醒时增加 `skip_count`。
* 普通暂停和今天免打扰成功执行时增加 `pause_count`。
* 后台运行、鼠标轻微移动、键盘输入和系统窗口抢前台不参与统计判断。
* `立即休息`不增加计划提醒次数，也不抬高计划提醒完成率。
* 程序退出或异常中断导致的未结束提醒不增加完成次数。

### 10.3 设置窗口展示

在设置窗口中增加简单统计区域：

```text
计划提醒：12 次
倒计时完成：9 次
主动跳过：2 次
暂停：1 次
倒计时完成率：75%

[清空统计]
```

要求：

* 完成率分母为零时显示 `暂无数据`，不能除零。
* 清空统计需要确认。
* 清空统计不影响配置、悬浮窗位置和当前计时。
* 不制作图表、趋势、排行榜和在线同步。

### 10.4 涉及文件

```text
app/stats.py
app/paths.py
app/persistence.py
app/core/events.py
app/core/timer_engine.py
app/ui/bridge.py
app/settings_window.py
tests/test_stats.py
tests/test_timer_engine.py
tests/test_bridge.py
tests/test_settings_window.py
```

### 10.5 验收标准

* 每次计划提醒只增加一次提醒次数。
* 自然结束只增加完成次数。
* 主动跳过只增加跳过次数。
* 普通暂停和今天免打扰只增加暂停次数。
* 立即休息不污染计划提醒完成率。
* 重启后统计保留。
* 清空统计不影响其他本地状态。
* 损坏的 `stats.json` 不阻止启动。

## 11. 测试计划

### 11.1 自动化测试

每个阶段先写失败测试，再实现最小代码。最终运行：

```powershell
py -m pytest -q tests -p no:cacheprovider --basetemp=.tmp\pytest-v12
py -m compileall -q app main.py
git diff --check
```

### 11.2 手动 GUI 验收

至少验证：

* 设置窗口输入 `NaN`、`Infinity`、负数、超大值和空值。
* 提醒窗口的 `+/-`、5/15/30 分钟按钮和滚轮调节。
* 自然结束、主动跳过、暂停、退出四种结果的统计区别。
* 今天免打扰、程序重启、跨午夜恢复。
* 立即休息不污染计划提醒完成率。
* 悬浮窗在正常、普通暂停、离开、全屏和今日免打扰状态下的显示。
* Windows 不同 DPI 和至少一个多显示器环境下的现有悬浮窗行为。

## 12. 明日开发顺序

明天从阶段 1 开始，不同时开发多个阶段：

1. 检查当前工作区未提交修改，确认不覆盖现有工作。
2. 为配置边界、`NaN/Infinity` 和安全保存补失败测试。
3. 实现配置校验和原子 JSON 保存。
4. 运行阶段 1 测试和完整测试。
5. 只有阶段 1 测试通过后，才进入阶段 2。

## 13. 完成标准

v1.2 只有在以下条件全部满足后才算完成：

* 四个开发阶段的自动化测试通过。
* README 和 HANDOFF 已同步用户可见行为、命令和测试结果。
* 手动 GUI 验收完成且没有已知阻塞问题。
* 用户明确确认“验收没有问题”。
* 用户确认前不推送 GitHub。
